'use strict';

/* global angular */

// some handy variables
var app = {
  name: 'beerApp'
};

// make it Angular
var beerApp = angular.module(app.name, []);


// We aren't really doing anything appy yet!
